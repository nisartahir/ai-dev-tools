# AI Dev Tools Coursework

My projects and homework for the DataTalksClub AI Dev Tools Zoomcamp.

## Projects

- [`homework-1`](homework-1/) — Django household chore manager

Clone this repository together with its submodules:

```bash
git clone --recurse-submodules git@github.com:nisartahir/ai-dev-tools.git
```

If the repository was already cloned without submodules:

```bash
git submodule update --init --recursive
```
